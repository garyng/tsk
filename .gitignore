node_modules/
dist/
out/
out-test/
.vscode-test/
coverage/
# Playwright run output (the golden screenshots under tests/ ARE tracked).
test-results/
playwright-report/
# Track the artifacts/ directory itself (so build scripts can write into it
# without a prior mkdir) but ignore the .vsix outputs it accumulates.
artifacts/*
!artifacts/.gitkeep
*.vsix
*.log
