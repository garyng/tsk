<!-- demo file — every milestone updates this to exercise its features end-to-end -->

# tsk demo

This file is the living showcase of every implemented feature. Open it in the
Extension Development Host (`F5` from this folder) to see grammar highlighting,
decorations, code lenses, and commands in action.

## Markdown still works

Headings, **bold**, *italic*, `inline code`, and links all render via the embedded
markdown grammar.

```ts
// Fenced code blocks are highlighted by the embedded TypeScript grammar.
function hello(name: string): string {
    return `hi ${name}`;
}
```

```sh
echo "shell blocks too"
```

> Block quotes also work — markdown features are not lost.

## Task markers

Every marker character is scoped distinctly, so themes can color them per-marker.
Decorations on top of these scopes arrive in M4.

- [ ] todo — the default empty marker
- [/] in progress — work has started
- [x] completed — done
- [>] moved — relocated somewhere else
- [!] cancelled — abandoned
- [n] notes — informational sub-item

Case tolerance: `[X]` and `[N]` are also accepted on read.

- [X] also completed
- [N] also a note

Indented tasks (nesting) work too:

- [ ] parent task
    - [/] indented child
        - [x] deeply nested
- [ ] sibling

## Inline metadata

Metadata lives inside an HTML comment after the task content. Each `@key:value`
pair is scoped separately so themes can highlight the key, the separator, and
the value.

- [x] write the grammar <!-- @id:k3w8t9rx @created:2026-05-24T15:00:00+08:00 @completed:2026-05-24T17:30:42+08:00 -->
- [/] try a value-less flag <!-- @id:m2x7v4qy @starred -->

## Tags

Tags use `#word` and support `/` for hierarchy. Markdown headings (which start
with `# ` followed by a space) are *not* matched as tags.

- [ ] ship the M1 milestone #project/tsk #JIRAID-123
- [ ] wire up the homelab #inventory/homelab/nas1

## Combined example

A real task usually has a marker, content, tags, and metadata together:

- [/] integrate `node:sqlite` cache layer #project/tsk #milestone/M3 <!-- @id:p4f8c2dh @created:2026-05-24T17:45:11+08:00 @started:2026-05-25T09:00:00+08:00 -->

## Parser model (M2)

The parser reads every line above into a `Task` record exposing `marker`,
`indent`, `content`, an insertion-ordered `metadata` Map, a `tags` array, and
a zero-indexed `line`. Nothing visible yet — M3 plugs this into a SQLite cache
and surfaces it via commands — but the model is exercised by these examples.

### Full lifecycle

A task that has run through every status from creation to completion:

- [x] ship M2 parser #project/tsk #milestone/M2 <!-- @id:r7p3v2qm @created:2026-05-24T09:00:00+08:00 @started:2026-05-24T11:00:00+08:00 @completed:2026-05-24T18:30:00+08:00 -->

### Relationship metadata

Relationships are inline `@key:<task-id>` references. Codelens wiring lands in
M9; the parser already extracts these into the metadata Map.

- [ ] design caching schema #project/tsk <!-- @id:s8m4d2tr @parent:p4f8c2dh @relatedTo:r7p3v2qm -->
- [ ] write migration runner #project/tsk <!-- @id:t9n5e3us @dependsOn:s8m4d2tr -->
- [>] explore alt SQLite bindings <!-- @id:u1o6f4vt @movedTo:s8m4d2tr @moved:2026-05-24T20:00:00+08:00 -->

### Edge cases the parser must handle

- [x] a task whose value-less flag survives round-trip <!-- @id:v2p7g5wu @flag -->
- [x] a task whose empty-value entry survives round-trip <!-- @id:w3q8h6xv @flag: -->
- [x] **bold** and *italic* and `code` inside content render via the embedded markdown grammar <!-- @id:x4r9i7yw -->
